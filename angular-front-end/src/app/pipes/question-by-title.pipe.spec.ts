import { QuestionByTitlePipe } from './question-by-title.pipe';

describe('QuestionByTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionByTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
